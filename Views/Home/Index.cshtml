@model HomeModel    
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Bem vindos, @Model.Nome!</h1>
    <p>Meu primeiro site em MVC</p>
    <span>Seu email é: @Model.Email!</span>
</div>
